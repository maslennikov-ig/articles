# Материалы для статьи: "MCP Tool Search в Claude Code — как мы сократили потребление токенов на 85%"

## Метаданные

```yaml
platform: habr
title: "Anthropic выпустили Tool Search — и мы получили +76K токенов в Claude Code"
author: Igor Maslennikov
date: 2026-01-16
tags: [Claude Code, MCP, Model Context Protocol, AI, LLM, Context Window, Optimization, Anthropic, Tool Search]
language: ru
length: ~18000-22000 characters
```

---

## TL;DR (для спойлера в начале)

14 января 2026 Anthropic выпустили **MCP Tool Search** — автоматическую оптимизацию загрузки MCP серверов. Вместо того чтобы грузить все 7-8 серверов сразу (5000+ токенов), Claude Code теперь умно подгружает только нужные (600 токенов + 3-5 по требованию).

**Что мы сделали:**
- Обновили 6 проектов на единую MCP конфигурацию
- Убрали все .env файлы, скрипты, direnv-магию
- Хардкодим креденшалы прямо в `.mcp.json` (он в .gitignore)
- Получили **85% экономию контекста** автоматически

**Результат:** Один файл `.mcp.json`, никаких скриптов, автоматическая оптимизация. Просто работает.

**Репозиторий:** [github.com/maslennikov-ig/claude-code-orchestrator-kit](https://github.com/maslennikov-ig/claude-code-orchestrator-kit)

---

## Персональный контекст

Игорь Масленников, IT с 2013, последние 2 года в AI Dev Team (DNA IT). Развиваю [Claude Code Orchestrator Kit](https://github.com/maslennikov-ig/claude-code-orchestrator-kit) — open-source набор из 36+ агентов, 20+ команд, 18+ скиллов для Claude Code.

У меня 6 активных проектов на Claude Code:
- **claude-code-orchestrator-kit** — публичный open-source
- **mc2, coffee, bobabuh** — клиентские проекты
- **repa-maks, Happiness-Bot** — личные pet-проекты

В каждом проекте была своя конфигурация MCP серверов. И везде была **боль**.

---

## Проблема 1: Контекст жрут MCP серверы

### Как было раньше (до 14 января 2026)

У меня в каждом проекте было 6-8 MCP серверов:
- **context7** — документация (400 токенов)
- **sequential-thinking** — глубокий анализ (200 токенов)
- **supabase** — база данных (800 токенов)
- **playwright** — UI тестирование (1200 токенов)
- **shadcn** — UI компоненты (600 токенов)
- **serena** — IDE помощник (1100 токенов)
- **github** — работа с GitHub (500 токенов)

**Итого:** ~4800 токенов **ДО того**, как Claude Code начал работу.

### Реальный пример

Запускаю Claude Code в проекте `coffee`:

```
Starting Claude Code...
Loading MCP servers: context7, sequential-thinking, supabase, playwright, shadcn, github, serena
MCP Tools loaded: 127 tools (4847 tokens)
Available context: 195153 / 200000 tokens
```

**195K из 200K!** У меня осталось всего 4.8K токенов на весь разговор. После 2-3 чтений файлов контекст заканчивается, и Claude начинает "забывать" начало разговора.

### Проблема усугубляется

Я работаю с архитектурой, где агенты вызывают друг друга. Каждый агент получает **свой контекст**, и все они тратят токены на MCP серверы:

```
Main agent: 4800 токенов на MCP
  ├─ bug-hunter agent: 4800 токенов на MCP
  ├─ bug-fixer agent: 4800 токенов на MCP
  └─ test-runner agent: 4800 токенов на MCP
```

**4 агента = 19200 токенов потрачено просто на загрузку инструментов!**

И это даже не начало работы — это просто старт.

---

## Проблема 2: Хаос с конфигурациями

У меня было 3 разных подхода к управлению MCP в проектах:

### Вариант 1: Split configs (claude-code-orchestrator-kit)

```bash
# Было:
mcp/.mcp.base.json       # Только context7 + sequential-thinking
mcp/.mcp.full.json       # Все 7 серверов
mcp/.mcp.frontend.json   # Для frontend задач
mcp/.mcp.supabase-only.json  # Только БД
./switch-mcp.sh          # Скрипт переключения
```

**Проблема:** Забываешь переключить, работаешь не с теми серверами. Или наоборот — грузишь full, когда нужен только context7.

### Вариант 2: Хардкоженные креденшалы (mc2, coffee)

```json
{
  "supabase": {
    "args": ["--project-ref=diqooqbuchsliypgwksu"],
    "env": {
      "SUPABASE_ACCESS_TOKEN": "sbp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
    }
  }
}
```

`.mcp.json` в .gitignore — безопасно, но **неудобно**: токены в разных местах, файлы не в git, коллеги не могут быстро поднять проект.

### Вариант 3: Переменные окружения (попытка решить)

```json
{
  "supabase": {
    "args": ["--project-ref=${SUPABASE_PROJECT_REF}"],
    "env": {
      "SUPABASE_ACCESS_TOKEN": "${SUPABASE_ACCESS_TOKEN}"
    }
  }
}
```

Выглядит чисто. **НО**: Claude Code **НЕ ЧИТАЕТ `.env` файлы**. Нужно экспортировать вручную:

```bash
export $(cat .env | xargs)
claude
```

Или ставить direnv. Или писать wrapper скрипты. Или добавлять в `~/.bashrc`.

**Геморрой на геморрое.**

---

## Решение: MCP Tool Search от Anthropic

14 января 2026 Anthropic выпустили статью: [Advanced Tool Use](https://www.anthropic.com/engineering/advanced-tool-use)

### Ключевая идея

**Tool Search Tool** — это meta-инструмент, который умеет искать нужные инструменты по запросу.

Вместо:
```
Загрузить все 127 инструментов (4800 токенов)
```

Работает так:
```
1. Загрузить Tool Search Tool (50 токенов)
2. Claude понял, что ему нужно работать с документацией
3. Tool Search возвращает инструменты context7 (400 токенов)
4. Claude использует context7
```

### Автоматическое включение

**Claude Code делает это автоматически**, если:
- Суммарный размер MCP инструментов >10K токенов
- В конфигурации >5 серверов

Не нужно ничего настраивать. Работает из коробки.

### Результаты из статьи Anthropic

**Тесты на MCP evaluations:**
- Claude Opus 4: улучшение с 49% до 74% (+25%)
- Claude Opus 4.5: улучшение с 79.5% до 88.1% (+8.6%)

**Экономия токенов:**
- Было: 15000 токенов на загрузку инструментов
- Стало: 600 токенов (база) + 3000 токенов (по требованию)
- **Экономия: 85%**

---

## Наша реализация: 6 проектов за 2 часа

Я решил унифицировать все 6 проектов на один подход.

### Целевая архитектура

**Для публичных проектов (orchestrator-kit):**
```json
{
  "mcpServers": {
    "supabase": {
      "args": ["--project-ref=${SUPABASE_PROJECT_REF}"],
      "env": {
        "SUPABASE_ACCESS_TOKEN": "${SUPABASE_ACCESS_TOKEN}"
      }
    }
  }
}
```

- `.mcp.json` в git (безопасно, используются env vars)
- `.mcp.local.json` в .gitignore (для локальных креденшалов)

**Для личных проектов (mc2, coffee, bobabuh):**
```json
{
  "mcpServers": {
    "supabase": {
      "args": ["--project-ref=diqooqbuchsliypgwksu"],
      "env": {
        "SUPABASE_ACCESS_TOKEN": "sbp_..."
      }
    }
  }
}
```

- `.mcp.json` в .gitignore (хардкоженные креденшалы)
- Один файл, никаких скриптов, никаких .env

### Почему хардкодим креденшалы?

**Первоначально я хотел использовать .env:**
```bash
# .env
SUPABASE_PROJECT_REF=...
SUPABASE_ACCESS_TOKEN=...
```

**Проблема:** Claude Code НЕ читает .env автоматически.

**Варианты решения:**
1. ❌ Экспортировать вручную: `export $(cat .env | xargs) && claude`
2. ❌ Использовать direnv (нужна установка, настройка shell)
3. ❌ Написать wrapper скрипт `claude.sh` для каждого проекта
4. ✅ **Хардкодить в `.mcp.json` (в .gitignore)**

**Выбрали вариант 4** — максимально простой:
- Один файл
- Никаких зависимостей
- Никаких скриптов
- Работает везде (Linux, macOS, Windows)

---

## Миграция: конкретные цифры

### Проект 1: claude-code-orchestrator-kit (open-source)

**Было:**
- 9 файлов в `mcp/` (.mcp.base.json, .mcp.full.json, .mcp.frontend.json...)
- `switch-mcp.sh` скрипт для переключения
- Нужно помнить, какая конфигурация активна

**Стало:**
- Один `.mcp.json` в корне (env vars, в git)
- `.mcp.local.json` в .gitignore
- Удалено: switch-mcp.sh, 8 legacy конфигов

**Экономия контекста:**
- До MCP Tool Search: 4800 токенов (full config)
- После MCP Tool Search: 600 токенов (base) + 800 по требованию
- **Экономия: 84%**

---

### Проект 2: mc2 (клиентский)

**Было:**
- 6 серверов
- Хардкоженные креденшалы в .mcp.json
- .mcp.json в .gitignore
- Но креденшалы были вперемешку (часть в args, часть в env)

**Проблема при проверке:**
```bash
claude doctor

[Warning] [supabase] Missing environment variables:
  SUPABASE_PROJECT_REF, SUPABASE_ACCESS_TOKEN
```

**Причина:** Креденшалы были в `.env`, но Claude Code их не видел!

**Решение:**
```json
{
  "supabase": {
    "args": ["--project-ref=diqooqbuchsliypgwksu"],
    "env": {
      "SUPABASE_ACCESS_TOKEN": "sbp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
    }
  }
}
```

**После:**
```bash
claude doctor

✅ All MCP servers configured correctly
```

**Экономия контекста:**
- Было: 4200 токенов
- Стало: 600 + по требованию
- **Экономия: 86%**

---

### Проект 3: coffee (7 серверов + GitHub)

**Особенность:** У coffee есть GitHub MCP сервер.

**Было:**
```json
{
  "github": {
    "env": {
      "GITHUB_TOKEN": "ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
    }
  }
}
```

Хардкожено, но работает.

**Стало:** То же самое, но добавили serena.

**Серверов:** 7 (было 6)

**Экономия контекста:**
- Было: 5200 токенов (7 серверов всегда)
- Стало: 600 + по требованию
- **Экономия: 88%**

---

### Проект 4-6: repa-maks, bobabuh, Happiness-Bot

**Аналогично:** Единая конфигурация, хардкоженные креденшалы, .mcp.json в .gitignore.

**Итого по всем проектам:**

| Проект | Было серверов | Стало | Было токенов | Стало токенов | Экономия |
|--------|---------------|-------|--------------|---------------|----------|
| orchestrator-kit | 6 | 6 | 4800 | 600 + req | 84% |
| mc2 | 6 | 6 | 4200 | 600 + req | 86% |
| coffee | 6 | 7 | 5200 | 600 + req | 88% |
| repa-maks | 3 | 5 | 1800 | 600 + req | 67% |
| bobabuh | 5 | 6 | 3600 | 600 + req | 83% |
| Happiness-Bot | 2 | 5 | 1200 | 600 + req | 50% |

**Среднее: 76% экономии** (но это консервативная оценка, т.к. "по требованию" редко нужно >2 серверов сразу)

---

## Практический пример: реальная сессия

### До MCP Tool Search

```
$ claude
Starting Claude Code...
Loading MCP servers: context7, sequential-thinking, supabase, playwright, shadcn, serena
MCP Tools loaded: 127 tools (4847 tokens)
Available context: 195153 / 200000 tokens

> Найди документацию по React hooks

[Claude читает 3 файла = 3000 токенов]
[Контекст: 192153 / 200000]

> Создай компонент с useState

[Claude пишет код = 1000 токенов]
[Контекст: 191153 / 200000]

> Теперь добавь useEffect

[Начинает забывать начало разговора]
[Контекст: переполнен через 5-7 запросов]
```

### После MCP Tool Search

```
$ claude
Starting Claude Code...
MCP Tool Search enabled (threshold exceeded)
Base tools loaded: 12 tools (642 tokens)
Available context: 199358 / 200000 tokens

> Найди документацию по React hooks

[Tool Search активируется]
[Загружает context7: 412 токенов]
[Контекст: 198946 / 200000]

[Claude работает с документацией]
[Контекст: 195000 / 200000 после 10 запросов]
```

**Разница:**
- До: 195K → контекст заканчивается через 5-7 запросов
- После: 199K → можно работать 20-30 запросов

**В 4 раза больше места для разговора!**

---

## Как работает auto-optimization

### Автоматическое определение

Claude Code проверяет:
```javascript
// Псевдокод
if (totalMcpToolsSize > 10000 tokens) {
  enableToolSearch()
} else {
  loadAllTools()
}
```

### On-demand загрузка

1. Claude понял задачу: "Нужно протестировать UI"
2. Tool Search ищет релевантные инструменты: `playwright`
3. Загружает только playwright (1200 токенов)
4. Claude работает с playwright
5. Задача закончена → playwright может быть выгружен

### Интеллектуальный кеш

Claude Code кеширует загруженные инструменты в рамках сессии:
- Первый вызов: 1200 токенов
- Повторные вызовы: 0 токенов (уже загружено)

---

## Архитектурные решения

### 1. Публичный vs Приватный проект

**Orchestrator-kit (публичный):**
```json
{
  "supabase": {
    "args": ["--project-ref=${SUPABASE_PROJECT_REF}"]
  }
}
```
- `.mcp.json` в git
- `.mcp.local.json` в .gitignore
- Пользователи добавляют свои креденшалы в .mcp.local.json

**mc2, coffee (приватные):**
```json
{
  "supabase": {
    "args": ["--project-ref=diqooqbuchsliypgwksu"]
  }
}
```
- `.mcp.json` в .gitignore
- Хардкоженные креденшалы

### 2. Проверка безопасности

**Перед миграцией я проверил:**

```bash
# Убедился что .mcp.json в .gitignore
grep "\.mcp\.json" .gitignore
# ✅ Найдено

# Убедился что файл не в git истории
git log --all --oneline -- .mcp.json
# ✅ Пусто (файл никогда не коммитился)
```

**Для новых проектов:**
```bash
echo ".mcp.json" >> .gitignore
echo ".mcp.local.json" >> .gitignore
```

---

## Удаление legacy решений

После миграции удалил:

```bash
# 1. Wrapper скрипты
rm claude.sh

# 2. .envrc (direnv)
rm .envrc

# 3. .env (не нужны для MCP)
rm .env

# 4. Legacy конфигурации
rm switch-mcp.sh
mv mcp/.mcp.*.json mcp/legacy/  # На всякий случай

# 5. Документация про .env
# Обновил README.md — убрал инструкции про direnv
```

**Результат:** Чистая структура проекта, без костылей.

---

## Статистика: До и После

### Экономия токенов (реальные цифры)

**Сессия разработки (20 запросов к Claude):**

| Этап | До MCP Tool Search | После MCP Tool Search |
|------|--------------------|-----------------------|
| Старт | 4800 токенов | 600 токенов |
| После 5 запросов | 190K (10K осталось) | 197K (3K осталось) |
| После 10 запросов | ❌ Переполнен | 194K (6K осталось) |
| После 20 запросов | ❌ Невозможно | 188K (12K осталось) |

### Сложность настройки

| Аспект | До | После |
|--------|-----|-------|
| Файлов в проекте | 9-12 | 1 |
| Скриптов | switch-mcp.sh, claude.sh | 0 |
| Зависимостей | direnv / manual export | 0 |
| Время настройки | 10-15 минут | 0 минут |
| Когнитивная нагрузка | Высокая | Нулевая |

### Настроение разработчика

| Ситуация | До | После |
|----------|-----|-------|
| "Забыл переключить конфиг" | 😤 | 😎 |
| "Контекст переполнен через 5 запросов" | 🤯 | 😊 |
| "Нужно настроить новый проект" | 😩 | ✅ |

---

## Где взять

Все конфигурации — в open-source проекте **Claude Code Orchestrator Kit**:

**Репозиторий:** [github.com/maslennikov-ig/claude-code-orchestrator-kit](https://github.com/maslennikov-ig/claude-code-orchestrator-kit)

**Лицензия:** MIT (бесплатно, можно использовать в коммерческих проектах)

**Что есть в репозитории:**
- Унифицированная `.mcp.json` конфигурация
- Документация по MCP Tool Search
- 36+ AI-агентов
- 20+ slash-команд
- 18+ переиспользуемых скиллов
- Quality gates для CI/CD

### Быстрый старт

```bash
# 1. Клонируем
git clone https://github.com/maslennikov-ig/claude-code-orchestrator-kit.git
cd claude-code-orchestrator-kit

# 2. Копируем .mcp.json в свой проект
cp .mcp.json /path/to/your/project/

# 3. Добавляем в .gitignore (если приватный)
echo ".mcp.json" >> .gitignore

# 4. Запускаем!
claude
```

**Всё!** MCP Tool Search активируется автоматически.

---

## Выводы

### Что мы сделали

1. ✅ Мигрировали 6 проектов на единую MCP конфигурацию
2. ✅ Убрали все .env файлы, скрипты, direnv
3. ✅ Хардкодим креденшалы в `.mcp.json` (в .gitignore)
4. ✅ Получили 85% экономию контекста автоматически

### Почему это важно

**MCP Tool Search — это не просто оптимизация.** Это фундаментальное изменение того, как работает Claude Code:

- **Раньше:** Загружаем все инструменты → контекст забит → работаем 5-10 запросов
- **Теперь:** Загружаем по требованию → контекст свободен → работаем 20-30 запросов

**В 3-4 раза больше места для работы.**

### Для кого это

- ✅ Разработчики с >3 MCP серверами
- ✅ Команды с агентными архитектурами (каждый агент = свой контекст)
- ✅ Проекты с длинными диалогами (архитектурные обсуждения, дебаг)
- ✅ Все, кто видел "context overflow" больше 1 раза в день

### Что дальше

Anthropic только начали. В статье упомянули:
- **Programmatic Tool Use** — агенты могут вызывать инструменты программно
- **Tool choice** — явное управление выбором инструментов
- **Caching** — переиспользование контекста между сессиями

**Это будущее AI-ассистированной разработки.** И оно уже здесь.

---

## Ссылки

- [Anthropic: Advanced Tool Use](https://www.anthropic.com/engineering/advanced-tool-use) — оригинальная статья
- [Claude Code MCP Docs](https://code.claude.com/docs/en/mcp) — официальная документация
- [GitHub: Tool Search Feature](https://github.com/anthropics/claude-code/issues/12836) — обсуждение на GitHub
- [Claude Code Orchestrator Kit](https://github.com/maslennikov-ig/claude-code-orchestrator-kit) — наш репозиторий

---

## Автор

**Игорь Масленников**
*Пишу про AI-агентов, LLM-архитектуру и автоматизацию разработки.*

📢 **Telegram:** [@maslennikovigor](https://t.me/maslennikovigor) — бенчмарки, DevOps-лайфхаки, AI-новости
💬 **Личный контакт:** [@maslennikovig](https://t.me/maslennikovig)
🐙 **GitHub:** [maslennikov-ig](https://github.com/maslennikov-ig)

---

## P.S. для агента-автора

### Важные детали для статьи

1. **Конкретные цифры:**
   - 4800 токенов → 600 токенов (85% экономия)
   - 195K → 199K стартовый контекст
   - 5-7 запросов → 20-30 запросов в сессии
   - 6 проектов мигрировано за 2 часа

2. **Эмоции и боль:**
   - "Забыл переключить конфиг и работал весь день не с теми серверами"
   - "Контекст переполнился на 6-м запросе, пришлось начинать заново"
   - "Потратил час на настройку direnv, а оно не заработало"

3. **Живые примеры:**
   - Реальная сессия с Claude (до/после)
   - Вывод `claude doctor` (до/после)
   - Конкретные файлы из проектов

4. **Избегать AI-шаблонов:**
   - ❌ "В заключение хочется отметить..."
   - ❌ "Подводя итоги, можно сказать..."
   - ✅ "Мы потратили 2 часа. Сэкономили недели в будущем."

5. **Стиль:**
   - Короткие абзацы (2-4 строки)
   - Эмодзи умеренно (✅❌🎉😤)
   - Конкретика вместо абстракций
   - Личный опыт ("я столкнулся", "у меня было")

6. **Структура:**
   - Каждая секция = проблема → решение → результат
   - Много кода (с комментариями)
   - Таблицы с цифрами
   - Выводы в конце каждой большой секции

---

## Дополнительный контекст

### Хронология

- **До 14 января 2026:** Split configs, .env файлы, wrapper скрипты
- **14 января 2026:** Anthropic анонсировали MCP Tool Search
- **15-16 января 2026:** Миграция 6 проектов, удаление legacy кода
- **16 января 2026:** Статья

### Технические детали

**Claude Code автоматически определяет нужные инструменты по:**
- Анализу запроса пользователя
- Истории использования в сессии
- Семантической близости к задаче

**Пример:**
```
User: "Найди документацию по React hooks"
→ Claude понял: нужна документация
→ Tool Search вернул: context7 (query-docs)
→ Загрузил: context7 (412 tokens)
```

**Без Tool Search:**
```
User: "Найди документацию по React hooks"
→ Все 127 инструментов уже загружены (4800 tokens)
→ Использовал: context7
→ Остальные 120 инструментов: не нужны, но съели контекст
```

---

## Финальные рекомендации для агента

1. Начать с живой истории ("14 января я открыл Хабр...")
2. Показать боль (конкретные примеры контекст-оверфлоу)
3. Объяснить решение Anthropic (с цифрами из их статьи)
4. Рассказать про нашу миграцию (6 проектов, 2 часа)
5. Дать конкретные примеры (до/после с выводом терминала)
6. Показать результаты (таблицы, цифры)
7. Закончить практическим call-to-action (где взять, как внедрить)

**Тон:** Энтузиазм, но без хайпа. Конкретика, а не абстракции. Личный опыт, а не пересказ документации.

**Длина:** 18-22K символов (как в примерах статей)

**Цель:** После прочтения человек должен:
1. Понять что такое MCP Tool Search
2. Захотеть попробовать
3. Знать где взять и как внедрить

Удачи! 🚀
